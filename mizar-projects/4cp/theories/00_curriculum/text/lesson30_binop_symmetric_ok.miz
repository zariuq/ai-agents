:: Lesson 30 (POSITIVE): tuple application — BINOP_1 style (mode + attribute)

environ
  vocabularies LESSON30, XBOOLE_0, SUBSET_1, TARSKI, ZFMISC_1,
               FUNCT_1, FUNCT_2, RELAT_1, XBOOLEAN, MARGREL1, STRUCT_0;
  notations   XBOOLE_0, SUBSET_1, TARSKI, ZFMISC_1,
               FUNCT_1, FUNCT_2, RELAT_1, XBOOLEAN, MARGREL1, STRUCT_0;
  constructors XBOOLE_0, SUBSET_1, TARSKI, ZFMISC_1,
               FUNCT_1, FUNCT_2, RELAT_1, XBOOLEAN, MARGREL1, STRUCT_0;
  registrations XBOOLE_0, SUBSET_1, FUNCT_1, FUNCT_2, RELAT_1, ZFMISC_1,
               XBOOLEAN, MARGREL1, STRUCT_0;
  requirements SUBSET, BOOLE;

begin

definition
  let X be non empty set;
  mode BinBoolOp of X is Function of [:X,X:], BOOLEAN;
  correctness;
end;

definition
  let X be non empty set;
  let F be BinBoolOp of X;
  attr F is symmetric-on means
  for a,b being Element of X holds F.[a,b] = F.[b,a];
end;

:: sanity use

theorem
  for X being non empty set, F being BinBoolOp of X st F is symmetric-on
  for x being Element of X holds F.[x,x] = F.[x,x];
proof
  let X be non empty set, F be BinBoolOp of X;
  assume F is symmetric-on;
  let x be Element of X;
  thus F.[x,x] = F.[x,x];
end;

end;
