CakeML MeTTa capacity prototype status
======================================

Current date: 2026-06-01

This file tracks the broad easy examples requested before choosing the final
MeTTa structure/design.

Implemented executable prototypes
---------------------------------

1. M1 pure symbolic kernel
   - SML: /home/zar/claude/CakeML/metta-ref/sml/metta_m1.sml
   - CakeML: /home/zar/claude/CakeML/metta-ref/cml/metta_m1.cml
   - Tests: /home/zar/claude/CakeML/metta-ref/tests/m1_tests.sml

2. Broad capacity witnesses
   - SML harness: /home/zar/claude/CakeML/metta-ref/tests/capacity_prototypes.sml
   - MeTTa witness catalog: /home/zar/claude/CakeML/metta-ref/tests/capacity_prototypes.metta
   - Local oracle runner: /home/zar/claude/CakeML/metta-ref/oracle/run_local_oracle.sh
   - CeTTa HE golden runner: /home/zar/claude/CakeML/metta-ref/oracle/run_cetta_goldens.sh

3. Direct .metta script runners
   - SML runner: /home/zar/claude/CakeML/metta-ref/tests/run_metta_file.sml
   - CakeML runner generator: /home/zar/claude/CakeML/metta-ref/tools/make_cml_runner.sh
   - Curated local scripts: /home/zar/claude/CakeML/metta-ref/tests/curated/
   - SML/CakeML runner oracle: /home/zar/claude/CakeML/metta-ref/oracle/run_curated_local.sh
   - Selected CeTTa comparison oracle:
     /home/zar/claude/CakeML/metta-ref/oracle/run_selected_cetta_compare.sh

Covered by runnable tests
-------------------------

- Accepted parser subset in SML and CakeML: comments/whitespace, symbol,
  variable, integer/string, nested expression, empty expression, !-prefixed
  Run command, multi-command program, and malformed-expression rejection.
- Runner behavior: add atoms to &self, then evaluate a query against them.
- Metatype witness: Symbol/Variable/Expression/Grounded distinction.
- Atom matching with repeated-variable constraints.
- Equality-driven evaluation.
- Minimal instructions: eval, chain, unify, decons-atom, cons-atom,
  function/return, plus an evalc-like typed-evaluation sketch.
- Result handling: Empty filtering, NotReducible, Error, fuel overflow.
- Branch handling: collapse, superpose, bag comparison.
- Integer/boolean primitive table and BadArgType witness.
- Host-call naming prototype: call-ml has a finite local inc witness, and
  call-native fronts to the same local dispatcher in this implementation
  profile while unknown host calls remain NotReducible.
- Type declaration lookup and gradual Atom/%Undefined% match sketch.
- Typed calls: Nat/Add positive and bad-argument negative witnesses.
- Dependent type sketch: Vec/Cons result type witness.
- Meta-type evaluation-order witness: Atom argument is not evaluated.
- Standard controls: case, switch, let*, quote/noeval, collapse-bind and
  superpose-bind witnesses.
- Substitution-shape witnesses in both SML and compiled CakeML: quote-aware
  substitution, token-splicing substitution, positional substitution, and
  fuelled dereference over chained bindings and self cycles.
- Explicit named spaces: add, remove, match, frame example.
- Explicit states: get after change.
- Deterministic module import sketch.
- Open primitive/call-native boundary: unknown external call returns
  NotReducible in the prototype.
- Direct execution of curated .metta files in both SML and compiled CakeML.
- Selected SML/CakeML-vs-CeTTa output comparisons for simple rewrite,
  pattern-variable substitution, unknown-expression preservation, and basic
  eval.
- Selected SML/CakeML-vs-CeTTa comparisons now cover 31 files total. The
  normalized-bag subset includes duplicate multiplicity, nondeterministic
  equality, match branching, chain branching, nested-chain cross products,
  evaluated argument arithmetic, nondeterministic if/let, evaluated match
  templates, collapse, and evaluated superpose. The selected set now also
  includes direct case/switch branch scripts, switch variable-pattern binding,
  a switch no-evaluate-scrutinee check, sequential let*, and function/return
  through chain. The exact-output subset now includes get-type comparisons for
  direct declarations, simple function-result types, the dependent Vec/Cons
  witness, context-space matching, and the official evalc context-space
  script.
- CakeML FFI byte-protocol witness:
  /home/zar/claude/CakeML/metta-ref/ffi/ffi_probe.cml and
  /home/zar/claude/CakeML/metta-ref/ffi/ffi_probe.c exercise call-ml and a
  call-native-to-call-ml front tag over a small mutable byte request/response.
  The byte protocol now covers inc, dec, and echo opcodes.

Proof foothold
--------------

HOL4 theory:
- /home/zar/claude/CakeML/metta-ref/hol/metta_m1Script.sml
- /home/zar/claude/CakeML/metta-ref/hol/substitution_zooScript.sml

Currently proves:
- atom equality decidability;
- error atom recognition;
- visible_results does not contain Empty;
- visible_results membership is drawn from the input;
- lookup_bind hit/miss facts;
- one-step substitution facts for variables;
- a structurally terminating arbitrary-depth matcher using atom/list depth as
  the termination measure, replacing the earlier bounded wrapper shape;
- match facts for variables, symbols, repeated-variable positive/negative
  examples, and a nested-expression variable witness;
- match_space member soundness;
- equality_step monotonicity under consing a space;
- eval_equalities member soundness against equality_step;
- a first fuel-indexed evaluator shape theorem;
- equality-step soundness for non-identity fuel evaluator results;
- an expanded eval_m1 theorem covering equality evaluation and match-space
  calls;
- match-space member soundness for eval_m1 match calls;
- identity equality-rule and match-call evaluation examples.
- an extended one-step HOL evaluator branch cluster for let, if, integer
  addition, multiplication, subtraction, equality, integer comparison,
  boolean and/or/not, unify, decons-atom, cons-atom, collapse, superpose,
  finite call-ml/call-native host-call witnesses, and primitive bad-argument
  errors, with a result-classification soundness theorem and concrete
  positive/negative examples.
- a recursive HOL evaluator prototype layered over eval_m1_ext for
  evaluated arithmetic/boolean/equality primitive arguments, function/return,
  eval/not-reducible, and chain, with concrete positive and negative examples;
  recursive add now has nonempty lemmas where both evaluated argument result
  lists are nonempty, and recursive add/multiply/subtract/less-than/equality
  have input-sensitive member soundness lemmas connecting each successful
  output to members of the evaluated argument result lists. Boolean and/or/not
  now have analogous input-sensitive result provenance, and function/return
  plus chain have member-soundness and nonempty bridge lemmas.
- a HOL finite type-call witness for Add/Nat: declared-or-default type lookup,
  type matching, typed Add rejection for bad arguments, and typed Add success
  through the equality evaluator.
- HOL evalc, dependent Vec/Cons, case, switch, and full fuelled multi-binding
  let* branches are now integrated into the main recursive evaluator. Evalc,
  case, and switch have branch-level member/provenance lemmas; Vec/Cons and
  let* have direct evaluator branch/evaluation witnesses, including a
  sequential two-binding let* example. Full fuelled let* now also has a
  recursive provenance predicate plus an iff theorem connecting exactly the
  members of eval_let_star_with to that provenance predicate, lifted to the
  eval_m1_rec let* branch.
- HOL host-call profile predicates classifying call-ml/call-native inc as the
  finite supported host profile while other host names remain NotReducible;
  the finite profile now has case-splitting and builtin-evaluation boundary
  theorems for integer success and bad-argument rejection, plus a theorem that
  call-native and call-ml agree on the supported integer-success profile.
- evaluator-level evalc type soundness: any non-error result from the
  integrated evalc branch matches the requested expected type under the
  declared-or-default type lookup; the branch now also has an explicit
  typed-or-type-error classification theorem.
- recursive evaluator branch soundness: arithmetic, comparison, equality,
  booleans, function/return, recursive eval, chain, evalc, Vec/Cons, case,
  switch, and full let* are folded into a common branch-result predicate.
  Valid case and switch results now carry branch/match provenance; invalid
  chain, case, switch, and let* forms are classified as type errors; Vec/Cons
  is exact; let* carries fuelled sequential provenance; and fuel-zero timeout
  has a separate soundness theorem.
- fuller recursive evaluator result soundness: the top-level branch predicate
  now matches the evaluator dispatch shape; non-branch atoms have an explicit
  fallback equality theorem to eval_m1_ext, and eval_m1_rec_result_sound covers
  fuel-zero timeout, proven recursive branches, and eval_m1_ext fallback
  results.
- intended nonempty/totality facts now cover recursive multiply, subtract,
  less-than, equality, evalc under typed-error-or-nonempty-input conditions,
  Vec/Cons, invalid chain/case/switch/let* forms, and scoped case/switch
  branches when a selected branch body is nonempty.
- let* success-path nonempty now follows directly from let_star_provenance,
  both for eval_let_star_with and for the integrated eval_m1_rec let* branch.
- input-sensitive branch soundness is split into arithmetic/comparison and
  boolean branch families, with refinement theorems back into the common
  branch-result predicate.
- host-call byte-profile boundary: the finite call-ml/call-native inc profile
  now has a HOL byte-response model matching the FFI probe shape for call-ml
  and the call-native-to-call-ml front tag, including supported-call response
  classification and unknown-conf rejection.
- control and structural branch families are now explicit in HOL: control
  covers function/return, recursive eval, and chain; structural covers evalc,
  Vec/Cons, case, switch, and full let*. Each family has an input theorem from
  the recursive evaluator and a refinement theorem back into the common branch
  soundness predicate.
- host-call dispatch has a finite HOL table for the supported call-ml and
  call-native inc profiles, with lookup examples, lookup soundness, profile
  completeness for supported inc calls, and a table-to-byte-boundary theorem
  connecting table lookup, host profile classification, eval_m1_ext output,
  and byte-response shape.
- recursive evaluator family result soundness now lifts arithmetic/comparison,
  boolean, control, and structural family predicates into a stronger
  all-fuel theorem, while retaining the explicit eval_m1_ext fallback for
  non-family atoms.
- control/structural family input-sensitive soundness now has branch-specific
  theorems for chain, evalc, case, switch, and let*. These expose the
  evaluated nested value, typed evalc value, selected case/switch branch, or
  let* sequential provenance directly at the family layer.
- scoped nonempty facts now include return and recursive eval under nonempty
  input, alongside the existing function/return-payload, arithmetic/boolean,
  evalc, Vec/Cons, chain, case/switch, and let* conditions.
- host-call byte profiles now include a finite byte table for inc, dec, and
  echo on both call-ml and the call-native-to-call-ml front tag. HOL proves
  concrete lookup examples, lookup soundness/completeness against the byte
  profile predicate, supported-opcode responses, unknown-opcode rejection for
  supported host configs, and a table-to-byte-response boundary theorem.
- host-call byte profiles are now separated from evaluator-level host calls:
  HOL classifies inc as the evaluator-level host profile, while dec and echo
  remain byte-only supported profiles. This keeps executable byte-protocol
  probes from silently expanding the evaluator semantics.
- a first HOL-to-CakeML representation bridge is checked: exported_atom,
  export_atom/import_exported_atom, export_eval_m1_rec, and
  import_export_eval_m1_rec prove that exported recursive evaluator results
  import back to exactly the HOL result list. The bridge note is
  /home/zar/claude/CakeML/metta-ref/notes/hol_to_cakeml_extraction_bridge.txt.
- nested family soundness is now proved for chain, evalc, case, switch, and
  let*. Chain/case/switch expose family-soundness for the evaluated selector
  and selected branch result; evalc exposes family-soundness when it evaluates
  the term rather than taking the typed-error guard; let* records
  family-soundness through a recursive sequential provenance predicate.
- a tiny checked HOL-to-executable fragment now covers the return branch:
  eval_return_fragment agrees with eval_m1_rec on return atoms, exported
  return-fragment results import back exactly, and matching SML/CakeML tests
  exercise the same import/export path.
- the nested branch-specific family theorems for chain, evalc, case, switch,
  and let* are consumed by a common nested-family theorem over the five valid
  branch shapes.
- a second tiny checked HOL-to-executable fragment now covers evaluated
  integer addition: eval_add_fragment agrees with eval_m1_rec on `+` atoms,
  exported add-fragment results import back exactly, and matching SML/CakeML
  tests exercise evaluated arguments through the import/export path.
- the common nested-family theorem over chain, evalc, case, switch, and let*
  is consumed by a fuller recursive-result theorem, so every recursive result
  still satisfies the general result-soundness predicate, and valid nested
  family atoms additionally expose their branch-specific nested-family
  evidence.
- switch semantics are aligned with the HE/CeTTa behavior: switch performs
  direct pattern matching against the scrutinee atom, binds pattern variables,
  and does not evaluate the scrutinee before branch selection. Case remains
  the evaluated-selector control form. The HOL, SML, and CakeML evaluators
  now follow this split, with CeTTa comparison scripts for both direct
  variable-pattern matching and no-evaluate-scrutinee behavior.
- checked HOL-to-executable fragments now also cover recursive eval, chain,
  and simple case: eval_eval_fragment, eval_chain_fragment, and
  eval_case_fragment each agree with eval_m1_rec on its covered branch, each
  exported fragment imports back exactly, and matching SML/CakeML tests
  exercise those import/export paths.
- fragment parity is now lint-checked for the SML and CakeML mirrors:
  /home/zar/claude/CakeML/metta-ref/tools/check_fragment_exports.py no longer
  owns semantic fragment bodies. It derives the fragment inventory from the
  HOL theory and checks that every HOL fragment has export, import/export, and
  semantic-agreement theorems and appears in the marked SML/CakeML fragment
  blocks.
- the bridge theory now includes a translator-backed pure chain payload:
  bridge_chain_subst_values uses a first-order bridge copy of lookup and
  substitution, proves that copy equal to lookup_bind/apply_subst, obtains
  CakeML translator and app-spec certificates, translates the result-row
  concatenation helper bridge_chain_concat_results, and proves that the full
  HOL chain branch decomposes into substituted templates, recursive evaluation
  of each substituted template, and concatenation of those result rows.
- the bridge theory now starts the same pattern for match: bridge copies of
  bind_var, match_atom/match_list, and match_space_payload are proved equal to
  the HOL semantic functions, have positive and negative matcher examples,
  prove bridge_eval_match_fragment agrees with eval_m1_ext/eval_m1_rec on the
  match branch, and obtain CakeML translator/app-spec certificates for the pure
  payload and match fragment.
- the bridge theory now adds tiny CF wrappers over translated numeric-world
  functions: bridge_eval_return_fragment_hand calls the translated
  eval_return_fragment, and bridge_eval_match_fragment_hand calls the
  translated bridge_eval_match_fragment. HOL proves CF app specs for both.
  The match translation also emits
  /home/zar/claude/CakeML/metta-ref/generated/bridge_eval_match_fragment.sexp
  as an auditable translated-code artifact.
- the bridge now lifts the bridge_surface_atom return-core behavior through
  the numeric eval_return_fragment: bridge_surface_eval_return_fragment_core
  is proved to import to eval_return_fragment whenever the surface atom imports
  successfully. The string-boundary return predicate
  bridge_surface_is_return_symbol_hand is directly CF-proved, and
  /home/zar/claude/CakeML/metta-ref/tools/check_generated_match_sexp.sh now
  consumes the generated match s-expression with the CakeML compiler's
  s-expression mode as a harness check.
- the bridge now defines a source-string atom layer, bridge_source_atom, and a
  bridge_source_surface_rel relation to bridge_surface_atom through explicit
  variable/string environments. HOL proves positive/negative examples and
  bridge_source_eval_return_fragment_core_refines_surface, so the source-level
  return core refines the surface-level return core under the relation. The
  pure source-level return core is now also translated and has a tiny CF
  wrapper theorem, bridge_source_eval_return_fragment_core_hand_spec.
- the bridge now defines a prototype atom mirror, bridge_proto_atom, for the
  handwritten Sym/Var/Int/Str/Expr atom shape. HOL proves
  bridge_proto_eval_return_fragment_core_refines_source, translating the
  prototype return-block shape into the source-string return core, and adds a
  translated/CF-backed wrapper theorem,
  bridge_proto_eval_return_fragment_core_hand_spec.
- the parser bridge now has a token-boundary theorem:
  bridge_parse_atom_token_source_surface_related proves that related
  bridge_source_token/bridge_token atom tokens parse to related
  bridge_source_atom/bridge_surface_atom values and related token remainders.
  It also has a checked nested positive witness,
  bridge_parse_source_surface_full_expr_positive_example, for a source
  `(return $x)` token list and the corresponding surface token list. The
  full fuel-indexed and full-token-list simulations are now proved by
  bridge_parse_source_surface_tokens_fuel_related and
  bridge_parse_source_surface_full_tokens_related.
- the parser bridge now lifts the token-boundary theorem through the command
  and program layers. bridge_parse_command_source_surface_tokens_fuel_related
  covers `!` run commands and ordinary add-atom commands;
  bridge_parse_program_source_surface_tokens_fuel_related covers
  multi-command token-list programs. The umbrella theorem
  bridge_source_surface_token_list_parser_simulation packages the atom,
  command, and program simulations into one full token-list/parser statement.
  Positive/negative witnesses cover a `!(return $x)` command, a mixed add/run
  program, and an unrelated variable token.
- the parser token-boundary slice now also has proof-bearing executable entry
  points: bridge_parse_atom_token, bridge_parse_atom_tokens_fuel,
  bridge_parse_source_atom_token, bridge_parse_source_atom_tokens_fuel,
  bridge_parse_command_tokens_fuel, bridge_parse_program_tokens_fuel,
  bridge_parse_source_command_tokens_fuel, and
  bridge_parse_source_program_tokens_fuel have translator certificates,
  app-spec certificates, and tiny CF wrapper theorems. For the mutually
  recursive full atom parsers, the app spec uses the atom-parser conjunct of
  the translation theorem.
- the bridge now has surface evaluated primitive-values wrappers:
  bridge_surface_eval_add_values_wrapper imports already-evaluated surface
  argument result lists, agrees with the numeric rec_add_values semantics when
  imports succeed, has positive/negative examples, and has translator/app-spec
  plus CF wrapper certificates through
  bridge_surface_eval_add_values_wrapper_hand_spec.
- the same surface wrapper pattern is now checked for <, ==, and/or/not:
  bridge_surface_eval_lt_values_wrapper,
  bridge_surface_eval_eq_values_wrapper,
  bridge_surface_eval_and_values_wrapper,
  bridge_surface_eval_or_values_wrapper, and
  bridge_surface_eval_not_values_wrapper import already-evaluated surface
  argument result lists, prove agreement with the corresponding rec_*_values
  helper, and have translator/app-spec/CF wrapper certificates. These are not
  yet full recursive surface evaluator branches.
- the recursive primitive branch surface layer has started:
  bridge_surface_eval_lt_fragment_wrapper,
  bridge_surface_eval_eq_fragment_wrapper,
  bridge_surface_eval_and_fragment_wrapper,
  bridge_surface_eval_or_fragment_wrapper, and
  bridge_surface_eval_not_fragment_wrapper import surface branch calls and
  prove agreement with eval_m1_rec for the corresponding recursive HOL branch
  shapes. These are HOL-level bridge theorems, not yet translated/CF
  executable wrapper certificates.
- the bridge now adds a surface-level match wrapper relation,
  bridge_surface_eval_match_fragment_rel, with soundness, positive/negative
  examples, and bridge_surface_eval_match_fragment_rel_agrees_with_eval_m1_rec
  connecting successfully imported surface match calls to the numeric
  recursive evaluator branch.
- the surface match wrapper is now executable and proof-bearing:
  bridge_surface_eval_match_fragment_wrapper imports surface inputs, calls the
  numeric bridge_eval_match_fragment when imports succeed, has relation and
  eval_m1_rec agreement theorems, and has a tiny CF wrapper theorem,
  bridge_surface_eval_match_fragment_wrapper_hand_spec.
- the generated match s-expression now has a checked HOL manifest:
  bridge_eval_match_fragment_artifact_manifest_checked plus
  bridge_eval_match_fragment_artifact_manifest_points_to_match_branch. The
  manifest theorem points to the existing match-branch semantic theorem rather
  than treating the file manifest as a semantic source of truth.
- the surface match wrapper now has a dynamic-symbol environment variant:
  bridge_surface_eval_match_fragment_wrapper_with_env imports match space and
  query atoms through bridge_import_surface_atom_with_env, proves relation and
  eval_m1_rec agreement theorems, has positive/negative dynamic-symbol
  witnesses, and has translator/app-spec/CF wrapper certificates.
- eval, case, switch, let*, and chain now also have env-aware surface HOL
  wrappers. Each wrapper imports its public surface syntax through the dynamic
  symbol environment and proves agreement with the corresponding eval_m1_rec
  branch. Chain additionally preserves the substituted-template provenance
  theorem through the env-aware wrapper. These control wrappers are not yet
  translator/CF-certified because the branch bodies close over eval_m1_rec or
  semantic fragments not yet translated in this bridge theory.
- /home/zar/claude/CakeML/metta-ref/notes/fragment_block_audit.txt now
  classifies every marked SML/CakeML fragment block. The audit intentionally
  classifies all marked blocks as exploratory only; translated helpers and
  numeric CF wrappers currently live in the bridge theory. The marked return
  block is now factored into a return-head predicate, an item-list helper, and
  the public eval_return_fragment_core wrapper, matching the proved factored
  bridge shape; it is still not a fully verified shipped string-world block
  until the atom relation proof is lifted through the executable code. The
  exact quoted prototype return-core pattern block is present in the bridge
  script, and its two branch payloads are now directly
  CF-proved as bridge_proto_return_singleton_hand_spec and
  bridge_proto_return_value_hand_spec. The whole-block dispatch proof is still
  open at the nested literal-pattern validate_pat/pmatch side condition. As a
  proof-bearing alternative, the bridge now defines
  bridge_proto_eval_return_fragment_core_factored, proves it extensionally
  equal to the original nested-pattern semantics, obtains translator/app-spec
  certificates for that factored definition, and CF-proves the corresponding
  factored handwritten body
  bridge_proto_eval_return_fragment_core_factored_body_hand_spec.
- the surface primitive-values bridge now also names the evaluated-argument
  addition boundary explicitly as bridge_surface_eval_add_evaluated_args_wrapper,
  with import/rec_add_values theorems, positive/negative examples, a CakeML
  translator certificate, an app-spec certificate, and the CF wrapper theorem
  bridge_surface_eval_add_evaluated_args_wrapper_hand_spec. This is still the
  already-evaluated argument-list boundary, not a proof of the recursive
  handwritten eval_add_fragment block.
- the same already-evaluated argument-list CF shape now has direct imported
  semantic postconditions for <, ==, and, or, and not:
  bridge_surface_eval_lt_values_wrapper_hand_rec_lt_spec,
  bridge_surface_eval_eq_values_wrapper_hand_rec_eq_spec,
  bridge_surface_eval_and_values_wrapper_hand_rec_and_spec,
  bridge_surface_eval_or_values_wrapper_hand_rec_or_spec, and
  bridge_surface_eval_not_values_wrapper_hand_rec_not_spec. These theorems
  state the rec_* semantics directly in the app postcondition after successful
  surface import.
- the <, ==, and/or/not surface primitive wrappers now also expose the
  recursive branch-fragment boundary in CF postconditions: when the imported
  already-evaluated argument lists are exactly the eval_m1_rec results for the
  branch arguments, the wrapper postcondition is the corresponding
  bridge_eval_*_fragment result. The same theorem family is present for
  dynamic-symbol environments.
- checked HOL-to-executable fragments now also cover type/evalc/dependent
  witnesses: eval_typed_fragment agrees with typed_eval_m1_rec,
  eval_evalc_fragment agrees with the integrated recursive evalc branch, and
  eval_vec_cons_fragment agrees with the integrated dependent Vec/Cons branch.
  Each exported fragment imports back exactly, with positive and negative
  SML/CakeML examples for typed Add and evalc result checking.
- the SML/CakeML runner now has a small get-type/type-capacity surface:
  direct declarations, simple function-result lookup, typed Add rejection, an
  evalc-type prototype, and the dependent Vec/Cons witness are executable.
  The get-type subset is compared against CeTTa on three selected scripts.
- the SML/CakeML runner now has a minimal HE evalc context-space surface:
  bind! of new-space creates a named local space, add-atom can extend that
  space, evalc can evaluate an atom in the named context, assertEqual can
  witness the result, and match can use context-space for &self. The selected
  CeTTa comparison set includes test_evalc.metta and test_context_space.metta.
- the supported evaluator host-call table is now explicitly separated from
  byte-only profiles: HOL proves supported table lookups are evaluator-level
  host profiles, evaluator host profiles have table entries, and byte-only
  profiles do not appear in the evaluator host-call table.
- the base SML/CakeML evaluator dispatch now includes case, switch, and let*
  branches, so curated CeTTa comparison scripts exercise those branch shapes
  directly rather than only through separate capacity helper functions.
- dynamic symbol environments now lift from symbol-level roundtrips to whole
  surface atoms and atom lists: bridge_export_surface_atom_with_env and
  bridge_export_surface_atom_list_with_env are inverse to the corresponding
  import functions under bridge_dyn_env_ok, and parser import success is tied
  to an exported surface atom by
  bridge_import_parsed_atom_tokens_with_env_export_sound.
- the return-core surface import theorem has also been lifted through dynamic
  environments: bridge_surface_eval_return_fragment_core_import_with_env proves
  return-fragment import correctness under bridge_dyn_env_ok, relying on
  bridge_import_symbol_with_env_return_iff to rule out dynamic aliases for the
  reserved return code.
- token-level command/program parsing now has an env-aware numeric command
  import/export layer: bridge_numeric_command, bridge_import_command_with_env,
  and bridge_export_command_with_env. HOL proves command and command-list
  roundtrips under bridge_dyn_env_ok, plus parsed-command/program export
  soundness through bridge_import_parsed_command_tokens_with_env_export_sound
  and bridge_import_parsed_program_tokens_with_env_export_sound, with positive
  Run/program and negative unknown-symbol witnesses.
- the command/program import helpers are now also in the CakeML
  translator/app-spec chain: bridge_import_command_with_env,
  bridge_import_command_list_with_env,
  bridge_import_parsed_command_tokens_with_env, and
  bridge_import_parsed_program_tokens_with_env have translator certificates;
  the parsed command/program import functions additionally have CF wrapper
  specs for the quoted CakeML wrapper functions.
- the command/program export helpers now have the same executable-facing
  foothold: bridge_export_command_with_env and
  bridge_export_command_list_with_env have translator certificates, app-spec
  certificates, CF wrapper specs, and positive/negative dynamic-symbol
  examples.
- evaluated-argument surface addition now also has an env-aware dynamic-symbol
  bridge: bridge_surface_eval_add_evaluated_args_wrapper_with_env imports the
  original and evaluated argument-result lists through bridge_dyn_env_ok-style
  environments, proves equality with rec_add_values when imports succeed,
  has dynamic positive/negative examples, and has translator/app-spec/CF
  wrapper certificates.
- the same env-aware evaluated-argument bridge pattern now covers the other
  primitive value-list wrappers: <, ==, and, or, and not. Each imports through
  the dynamic symbol environment, proves equality with the corresponding
  rec_*_values function when imports succeed, has dynamic positive/negative
  examples, and has translator/app-spec/CF wrapper certificates.
- let* step decomposition now has recursive payload soundness:
  bridge_eval_let_star_fragment_payload_recursive_sound exposes the evaluated
  first-binding value, the residual binding/body payload, residual
  eval_let_star_with membership, residual let_star_provenance, and residual
  let_star_family_provenance for every successful first-binding branch result.
- eval and evalc now have tiny surface helper wrappers over imported evaluated
  result/value lists. The wrappers are translated, have app-spec certificates,
  have quoted CakeML CF wrapper specs, and connect successful imports to the
  corresponding bridge_eval_eval_fragment/eval_evalc_fragment postconditions.
- The eval/evalc helper CF specs now also have recursive branch postconditions:
  bridge_surface_eval_payload_result_wrapper_hand_eval_m1_rec_spec and
  bridge_surface_evalc_checked_values_wrapper_hand_eval_m1_rec_spec state
  app-level results directly as eval_m1_rec branch results.
- The evalc and let* payload decompositions are now lifted through the common
  recursive soundness layer: bridge_eval_m1_rec_fuller_evalc_payload_sound and
  bridge_eval_m1_rec_fuller_let_star_payload_sound combine result soundness,
  nested-family soundness, family soundness, and the branch-specific payload
  witnesses.
- A shared bridge payload predicate now covers the supported nested recursive
  branch slices. bridge_eval_m1_rec_supported_nested_payload_sound dispatches
  chain, evalc, case, switch, and successful-head let* branch results into
  bridge_nested_payload_result; the let* case factors its pair-valued residual
  proof through bridge_let_star_payload_result_sound.
- The shared payload predicate is now consumed by a fuller recursive bridge
  theorem: bridge_eval_m1_rec_supported_nested_fuller_sound bundles ordinary
  result soundness, nested-family soundness, family soundness, and the
  bridge_nested_payload_result witness for every supported nested branch result.
- The next control branch cluster now has the same reusable bridge shape.
  bridge_eval_m1_rec_supported_control_fuller_sound covers function, return,
  and eval branch results, bundling ordinary result soundness, control-family
  soundness, family soundness, and a bridge_control_payload_result witness.
- The verified nested and control clusters are now combined behind one
  supported-branch theorem. bridge_eval_m1_rec_supported_branch_fuller_sound
  packages ordinary result soundness, family soundness, and the appropriate
  cluster-specific payload/soundness witness for chain/evalc/case/switch/let*
  plus function/return/eval.
- Non-proven fallback paths now have a bridge-consumable classification:
  bridge_eval_m1_rec_fallback_fuller_sound packages ordinary result soundness,
  family soundness, eval_m1_ext membership, and equality/builtin/match-space
  classification for atoms outside eval_m1_rec_proven_branch_atom. The theorem
  bridge_eval_m1_rec_supported_or_fallback_fuller_sound combines this fallback
  coverage with the already verified supported branch clusters.
- The match subfamily of fallback is now split out with stronger bridge
  evidence: bridge_eval_m1_rec_match_fallback_fuller_sound proves result
  soundness, family soundness, generic fallback classification, and the precise
  bridge_match_space_payload membership for `(match &self pattern templ)`.
- The equality-rewrite subfamily of fallback is now split out with stronger
  bridge evidence: bridge_eval_m1_rec_equality_fallback_fuller_sound covers
  non-proven, non-match, non-builtin atoms whose eval_equalities result is
  nonempty, tying outputs directly to eval_equalities membership and
  equality_step provenance.
- The builtin subfamily of fallback is likewise split out:
  bridge_eval_m1_rec_builtin_fallback_fuller_sound covers non-proven,
  non-match atoms for which builtin_eval returns a result list, tying outputs
  directly to builtin_step provenance while retaining generic fallback
  classification.
- The identity/default subfamily of fallback is now split out:
  bridge_eval_m1_rec_identity_fallback_fuller_sound covers non-proven,
  non-match, non-builtin atoms with no equality rewrites, tying the only output
  directly to the original atom while retaining generic fallback
  classification.
- The four fallback subfamilies are now packaged behind a single bridge
  theorem: bridge_eval_m1_rec_split_fallback_fuller_sound partitions each
  non-proven fallback atom into match, builtin, equality-rewrite, or identity
  fallback and returns the corresponding subfamily payload witness alongside
  ordinary result/family soundness and generic fallback classification.
- The supported-branch and split-fallback bridge facts are now consumed by a
  stronger combined theorem:
  bridge_eval_m1_rec_supported_or_split_fallback_fuller_sound keeps the
  supported branch payload for proven recursive branch clusters and uses the
  split fallback payload for every non-proven fallback result.
- A downstream modular evaluator-core bridge theory now consumes that theorem:
  metta_m1_bridge_eval_soundScript.sml defines
  bridge_evaluator_core_payload_atom/result and proves
  bridge_eval_m1_rec_evaluator_core_payload_sound. It includes a positive
  return example and a negative primitive-add example, making explicit that
  primitive proven branches still need their own integration rather than being
  silently included in this core predicate.
- The same downstream theory now adds primitive branch coverage:
  bridge_primitive_payload_atom/result covers arithmetic/comparison and
  boolean primitive families, with positive add and negative return examples.
  bridge_eval_m1_rec_primitive_payload_sound consumes the model's
  input-sensitive primitive soundness theorems, and
  bridge_eval_m1_rec_evaluator_payload_sound combines evaluator-core and
  primitive payload coverage behind one downstream predicate.
- The downstream evaluator theory now has a total HOL-model evaluator payload
  theorem: bridge_model_evaluator_payload_atom/result partitions all atoms into
  the model's proven family predicate or the split fallback predicate, proves
  bridge_model_evaluator_payload_atom_total, and proves
  bridge_eval_m1_rec_model_evaluator_payload_sound for every SUC-fuel
  recursive model result.
- notes/curriculum_coverage.tsv is now the checked curriculum coverage matrix.
  tools/check_curriculum_coverage.py verifies that each cited theorem, app-spec
  certificate, test, oracle script entry, file, and make target actually exists.
  This is an evidence registry, not an additional semantic proof.
- The parser-token HOL layer now has bridge_token_is_atom and
  bridge_token_is_atom_parse_singleton, connecting the token classifier for
  BTokAtom directly to successful singleton bridge_parse_atom_token behavior
  with positive and negative witnesses. This is HOL parser-boundary evidence,
  not a CF proof of the shipped parser.
- The parser-head slice now also has a custom parse-result kernel:
  bridge_parse_atom_token_result, bridge_parse_atom_token_result_success,
  translator/app-spec certificates, and bridge_parse_atom_token_result_hand_spec.
  This avoids the brittle polymorphic option CF boundary while still tying the
  result-shaped parser head to the existing bridge_parse_atom_token model.
- The atom-token import boundary now has
  bridge_import_parsed_atom_tokens_result_with_env and
  bridge_import_parsed_atom_tokens_result_with_env_success, plus
  translator/app-spec certificates and
  bridge_import_parsed_atom_tokens_result_with_env_hand_spec. This gives a
  concrete success/parser-error/import-error result shape for full atom-token
  parsing and dynamic-symbol import.
- The command-token import boundary now has
  bridge_import_parsed_command_tokens_result_with_env and
  bridge_import_parsed_command_tokens_result_with_env_success, plus
  translator/app-spec certificates and
  bridge_import_parsed_command_tokens_result_with_env_hand_spec. This mirrors
  the atom-token result shape for `!` run commands and add commands, keeping
  parser errors separate from dynamic-symbol import errors.
- The program-token import boundary now has
  bridge_import_parsed_program_tokens_result_with_env and
  bridge_import_parsed_program_tokens_result_with_env_success, plus
  translator/app-spec certificates and
  bridge_import_parsed_program_tokens_result_with_env_hand_spec. This lifts
  the explicit parse/import error split across full command lists.
- The raw character lexer slice now has bridge_tokenize_source_chars_fuel for
  whitespace, comments, parentheses, bang, decimal integers, variables,
  symbols, and string literals. It includes positive and negative witnesses,
  bridge_parse_source_program_chars_fuel, and
  bridge_source_chars_to_surface_program_parser_related, plus translator/app
  spec certificates and tiny CF wrappers over LIST_TYPE CHAR. This is still a
  verified char-list lexer model, not yet a CF proof of the shipped
  String.explode-based parser entrypoint.
- The source-string parser boundary now has bridge_tokenize_source_string_fuel
  and bridge_parse_source_program_string_fuel, implemented as mlstring
  `explode` into the verified char-list lexer/parser. It includes
  bridge_source_string_to_surface_program_parser_related, positive and negative
  string witnesses, translator/app-spec certificates, and tiny CF wrappers over
  STRING_TYPE. This proves the source-string model entrypoint, but still does
  not prove the exact shipped SML/CakeML parser functions.
- The public source-string parse-from-string result shape now has
  bridge_parse_source_{atom,command,program}_string_shipped. These model the
  shipped trailing-token behavior for atom/command parsing and use
  bridge_lex_error_message for lexer messages such as empty variable and
  unterminated string. Positive and negative witnesses cover atom, command,
  program, trailing-token, and lexer-error cases, and each entrypoint has a
  translator/app-spec certificate plus a tiny CF wrapper spec. This is still a
  proved model entrypoint over the verified fuelled lexer/parser, not a direct
  CF proof of the exact recursive shipped parser bodies.
- The same public source-string layer now has no-explicit-fuel bounded
  entrypoints:
  bridge_tokenize_source_string_bound and
  bridge_parse_source_{atom,command,program}_string_shipped_bound. These choose
  the lexer fuel from SUC (LENGTH (explode text)) and the token-parser fuel
  from SUC (2 * LENGTH toks), and have positive/negative parser witnesses,
  translator/app-spec certificates, and tiny CF wrapper specs.
  bridge_tokenize_source_chars_fuel_no_fuel_error and
  bridge_tokenize_source_string_bound_no_fuel_error prove that this chosen
  lexer bound cannot surface the synthetic lexer fuel error. The parser proof
  infrastructure includes bridge_parse_source_atom_tokens_fuel_rest_lt, which
  proves every successful source full-atom or expression-item parse returns a
  strict suffix of the input token list. It now also includes
  bridge_parse_source_atom_tokens_fuel_no_fuel_error_strong and
  bridge_parse_source_atom_tokens_double_bound_no_fuel_error, proving that
  source atom/expression-item token parsing cannot surface the synthetic parser
  fuel error under a doubled token-length bound.
  bridge_parse_source_atom_tokens_bound_no_fuel_error and
  bridge_parse_source_command_tokens_bound_no_fuel_error connect that doubled
  bound to the public source atom/command token entrypoints. The program
  parser now has bridge_parse_source_command_tokens_fuel_rest_lt,
  bridge_parse_source_program_tokens_fuel_no_fuel_error_strong, and
  bridge_parse_source_program_tokens_bound_no_fuel_error, proving the public
  source program-token bound cannot surface the synthetic parser fuel error.
  bridge_parse_source_atom_tokens_shipped_no_parser_fuel_error and
  bridge_parse_source_command_tokens_shipped_no_parser_fuel_error plus
  bridge_parse_source_program_tokens_shipped_no_parser_fuel_error lift these
  results to the shipped string-error result surface. The remaining parser-bound
  work is a direct CF proof of the exact shipped recursive parser bodies.
- The prototype/source parser bridge now has bridge_proto_token and
  bridge_proto_source_token_rel for the shipped-style Sym/Var/Int/Str/Expr
  string atom shape, plus bridge_parse_proto_source_full_tokens_related,
  bridge_parse_command_proto_source_tokens_fuel_related,
  bridge_parse_program_proto_source_tokens_fuel_related, and
  bridge_proto_source_token_list_parser_simulation. These prove that related
  prototype-token and source-token streams parse to related atoms, commands,
  and programs. The bridge now also has bounded no-explicit-fuel public
  entrypoints, bridge_parse_{source,proto}_{atom,command,program}_tokens_bound,
  plus bridge_proto_source_bound_parser_simulation, positive/negative bounded
  parser witnesses, translator/app-spec certificates, and tiny CF wrappers for
  those bounded entrypoints. The same boundary now has shipped-style
  string-error result wrappers:
  bridge_parse_{source,proto}_{atom,command,program}_tokens_shipped, with
  bridge_parse_error_message for the parser error strings, the relational
  theorem bridge_proto_source_shipped_parser_simulation, positive/negative
  shipped-result examples, translator/app-spec certificates, and tiny CF
  wrapper specs. The proto side now mirrors the source side's parser-fuel
  guarantees with proto atom/command/program rest-decrease and doubled-bound
  no-fuel theorems, plus shipped no-parser-fuel theorems for proto atom,
  command, and program token entrypoints. This is still a verified prototype
  parser model and result shape, not a direct CF proof of the exact shipped
  SML/CakeML parser code.

Substitution zoo foothold:
- substitution extension/refinement facts for association-list bindings;
- coverage invariants for substitutions over arbitrary nested atoms/lists;
- full arbitrary-depth match_sound: returned substitutions extend the input,
  cover the pattern variables, and instantiate the pattern to the matched
  subject;
- preservation of already-bound variables across fresh binding extension;
- explicit-substitution composition facts for inner/outer binding hits;
- fuelled variable dereference facts for chained bindings and self-cycle
  cutoff witnesses;
- branch-local binding trail and rollback facts for speculative match steps;
- occurs-check binding wrapper with positive and negative variable witnesses;
- Metamath-style token/list substitution with append and empty-substitution
  facts;
- positional substitution witnesses for de Bruijn-like variable indexing;
- quote-preserving substitution witness matching the PeTTa-to-HE translator's
  source-level "do not substitute under quote" behavior.
- first concrete constraint abstraction with equality, match, and abbreviation
  constraints; append/add preservation facts; positive/negative examples; and
  a proof that successful matching satisfies the match constraint.
- constraint queue/defer/simplify facts: simplification preserves exactly the
  same obligations under a substitution, deferred enqueue preserves the queue
  obligations, and covered solved constraints remain solved under later
  substitution extensions.
- rule-producing constraint simplification: equality/match/abbreviation
  constraints can produce binding extensions plus residual constraints or an
  explicit failure; queue-level simplification extends substitutions
  monotonically and is sound when residual constraints hold.
- occurs-check-aware rule-producing simplification and solved-form loop:
  direct and post-substitution cycles are rejected, successful loop results
  extend the input substitution, and the loop has a final-substitution
  preservation theorem when residual constraints hold.
- solved-form extraction from abbreviation constraints, with membership and
  soundness facts for extracted bindings.
- sequential solved-form extraction for occurs-checked residual abbreviation
  queues: extraction succeeds only when each residual right-hand side is
  covered by the current substitution, extends the current substitution,
  satisfies the extracted residuals, and composes with solved_form_loop into
  a final constraint-soundness bridge theorem.
- extractor-consuming matcher/evaluator bridge lemmas: successful matching,
  equality-step matches, and eval_m1 match calls can consume extracted residual
  abbreviation queues while preserving match constraints and residual
  constraint satisfaction under the final substitution.
- first evaluator-to-substitution bridge: equality steps instantiate their
  left-hand side, and eval_m1 match-call results instantiate the requested
  pattern.

Literature/source map:
- /home/zar/claude/literature/substitution_unification_sources_2026-05-30.txt

Next proofs
-----------

1. Strengthen the fuller recursive eval_sound story from result/family
   classification toward input-sensitive end-to-end provenance across nested
   recursive calls.
2. Extend eval_total/nonempty coverage to any remaining primitive/result
   constructors where the intended result set is nonempty.
3. Grow the HOL-to-CakeML bridge by moving pure helpers through
   HOL-definition-to-translator certificates, and reserve CF for wrappers,
   stateful code, and FFI boundaries.
4. Continue lifting public string/surface wrappers to executable fragments.
   The shipped-shaped factored return-core block is CF-proved over
   bridge_proto_atom; an exact shipped-structure proto return block now has a
   direct CF proof against the same core. The artificial one-pattern return
   block has been removed from the bridge surface so only proof-bearing
   wrappers remain. Token-list parser simulation covers source tokens to
   bridge tokens at atom, command, and program levels, and now also covers
   prototype Sym-string tokens to source tokens at atom, command, and program
   levels, including no-explicit-fuel bounded parser entrypoints. Surface evaluated-+
   wrappers have CF theorems that connect imported evaluated argument lists
   to the full add fragment. Case/switch now have a translated first-branch
   payload selector and value-list selector, with HOL decomposition theorems
   showing how the existing recursive branches factor into certified
   selection/substitution payloads plus recursive evaluation of the selected
   bodies. Tiny surface payload wrappers for case/switch are translator-backed
   and CF-proved, and the case/switch decomposition now feeds recursive
   result/family soundness for selected payloads. Eval, evalc, and let* also
   have first decomposition helpers: eval separates the "not reducible"
   post-processing, evalc separates the value/type filter, and let* separates
   one binding step into certified residual binding/body payloads. Eval/evalc
   helper wrappers now have CF specs with eval_m1_rec branch postconditions,
   and evalc/let* payload theorems are lifted through the common recursive
   soundness layer.
5. Expand evalc/type/dependent-type coverage beyond the minimal named-space
   evalc and typed Add surface toward richer dependent type unification.
6. Strengthen the finite call-ml/call-native byte-profile boundary toward a
   fuller external-call story.
7. Increase the selected CeTTa comparison set beyond 13 exact-output files plus
   19 normalized-bag files as output normalization and evaluator coverage grow.
8. Move from handwritten SML/CakeML parity toward a verified
   extraction/translation strategy.

Deferred from first verified kernel
-----------------------------------

- Raw-text lexer and human syntax error behavior. Token-list parsing now has a
  general source/surface simulation theorem, but raw source text does not yet.
- Full HE type system and dependent type inference.
- Full module/import filesystem behavior.
- Arbitrary host grounded atoms beyond the finite call-ml/byte-protocol
  witnesses.
- Floats.
- Documentation/assertion helpers.
