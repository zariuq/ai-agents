/-!
# Retired Legacy HOL Infinitary Consequence Name

The older countably infinitary consequence wrapper belonged to the predicate-code
fragment, not to the real Church-style HOL layer.

Use `Mettapedia.Logic.PLNWorldModelPredCodeInfinitaryCompleteness` instead.

This module intentionally re-exports nothing, so the public `HOL` namespace no
longer aliases predicate-code semantics.
-/
