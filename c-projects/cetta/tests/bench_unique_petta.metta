; PeTTa-source unique benchmark using the native list-level workaround surface.
; This is semantically equivalent to bench_unique_he.metta.

!(unique-atom (collapse (let $x (superpose (1 2 1 3 2)) (pair $x $x))))
